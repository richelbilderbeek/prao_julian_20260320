# Report

## Goal

> This paragraph is written by the supervisor

[The `scoreto` R package](https://github.com/richelbilderbeek/scoreto)
fails. Why?

- [ ] Clone the `scoreto` R package to your local computer
- [ ] In R Studio, open the `scoreto` project
- [ ] In R Studio, work on the branch called `develop`
- [ ] In R Studio, run the tests. There will be some that will fail

Find out why the tests fail. Do not touch the tests.
Fix the code. If all tests pass, you have succeeded.

## Steps
